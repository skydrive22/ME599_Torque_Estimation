import numpy as np
from pysindy import SINDy


def main():
    # Define your data
    t = np.linspace(0, 10, 100)
    x = np.sin(t)
    y = np.cos(t)

    # Fit the model
    model = SINDy()
    model.fit([x, y], t=t)

    # Print the model coefficients
    print(model.coefficients())
